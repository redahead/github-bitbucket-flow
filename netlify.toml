[build]
  command = "NITRO_PRESET=netlify bun run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# TanStack Start builds SSR via Nitro. The `netlify` preset emits static assets
# into `dist/` and a Netlify Function into `.netlify/functions-internal/`.